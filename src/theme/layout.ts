/** Max content width so screens don't stretch edge-to-edge on large tablets. */
export const CONTENT_MAX_WIDTH = 720;

/** Max width for wide catalog/list layouts (order items, product grids). */
export const WIDE_CONTENT_MAX_WIDTH = 960;
